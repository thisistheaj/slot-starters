{
  "name": "slot-starters",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "slot": "./index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "rm -rf build && mkdir -p build && cp -r site/* build/ && cp -r importable-starters build/ && echo 'Build completed in ./build'"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "commander": "^11.1.0",
    "dotenv": "^16.4.1",
    "mcp-js-server": "^0.1.3",
    "node-fetch": "^3.3.2",
    "openai": "^4.28.0",
    "tailwind-merge": "^3.0.1"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.0.8"
  }
}
